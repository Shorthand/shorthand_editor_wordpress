<?php
/**
 * Stands in for wp-admin/includes/file.php, which `FileSystem::init()`
 * requires before booting `WP_Filesystem`. The functions it defines are
 * stubbed in tests/bootstrap.php instead.
 */
